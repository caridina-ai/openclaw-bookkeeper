# 使用者帳目資料，不進版控
data/
# 安裝單元裡絕不能出現帳本：那會被 openclaw skills install 一起裝上去，
# 覆蓋掉使用場景的真實帳本。
bookkeeping/data/
__pycache__/
*.pyc
.pytest_cache/
# 維護現場檔（genesis/.ops）：私密 id ＋ 本機絕對路徑，換台機器就是另一份。
# **絕不進版控**，這是 genesis 通則，不是這個專案的特例。
# 不寫死 genesis/ 是因為別的專案可能放在別層——叫這個名字的一律擋掉。
.ops
.claude/
