# 使用者帳目資料，不進版控
data/
# 安裝單元裡絕不能出現帳本：那會被 openclaw skills install 一起裝上去，
# 覆蓋掉使用場景的真實帳本。
bookkeeping/data/
__pycache__/
*.pyc
.pytest_cache/
# 頻道現場檔（genesis/.tap，/tap skill 讀的那份）：裡面是頻道 id。
# **絕不進版控**——頻道位置洩漏出去，有心的人就能跑來騙機器人做事。
# 不寫死 genesis/ 是因為別的專案可能放在別層——叫這個名字的一律擋掉。
.tap
.claude/
